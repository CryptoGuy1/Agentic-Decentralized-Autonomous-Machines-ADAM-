# Core agent framework
crewai==1.1.0

# Vector DB client
weaviate-client==4.17.0

# LLM / reasoning client
openai==2.6.0

# Web API
fastapi==0.95.2
uvicorn[standard]==0.22.0

# Env + YAML + utils
python-dotenv==1.1.1
PyYAML==6.0
pydantic==2.3.0
httpx==0.24.1
requests==2.31.0

# gRPC support (weaviate uses grpc under the hood)
grpcio==1.58.0

# MQTT simulator client (if you use MQTT)
paho-mqtt==1.6.1

# Date parsing / validation / tests
python-dateutil==2.8.2
email-validator==1.3.1
pytest==7.4.0
